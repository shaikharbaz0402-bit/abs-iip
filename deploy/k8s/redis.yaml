apiVersion: apps/v1
kind: Deployment
metadata:
  name: abs-redis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: abs-redis
  template:
    metadata:
      labels:
        app: abs-redis
    spec:
      containers:
        - name: redis
          image: redis:7
          ports:
            - containerPort: 6379
---
apiVersion: v1
kind: Service
metadata:
  name: abs-redis
spec:
  selector:
    app: abs-redis
  ports:
    - protocol: TCP
      port: 6379
      targetPort: 6379
